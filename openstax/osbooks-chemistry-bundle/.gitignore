# Pristine OpenStax content fetched by ./fetch.sh and the toolchain overlaid by
# ./apply.sh -- regenerated, never committed (it's OpenStax's, and it comes back
# from the pin). This also covers checkout/output/ (the build artifacts).
checkout/

# Any stray top-level build output, and saved image tars.
output/
*.tar
