% Calculus theme override (loaded by osbook.cls via \InputIfFileExists).
% Restores the maintainer's calculus latex-branch look: a Times-like body face
% + a blue palette sampled from the official OpenStax Calculus PDF, replacing the
% default Roboto-Slab + teal. See tasks/reference/tooling/converters.md (gap #5).
\setmainfont{TeX Gyre Termes}          % Times-like serif (has a real italic)
\definecolor{OScolor}{HTML}{233A67}    % primary dark navy blue
\definecolor{OSmed}{HTML}{124479}      % medium blue (section headings)
\definecolor{OSaccent}{HTML}{528CBE}   % lighter blue pop
